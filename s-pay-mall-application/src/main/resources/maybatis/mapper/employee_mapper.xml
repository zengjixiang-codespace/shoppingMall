<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codespace.infrastructure.persistent.dao.IEmployeeDao">

    <resultMap id="EmployeeMap" type="com.codespace.infrastructure.persistent.po.Employee">
        <id column="id" property="id"/>
        <result column="employee_number" property="employeeNumber"/>
        <result column="employee_name" property="employeeName"/>
        <result column="employee_level" property="employeeLevel"/>
        <result column="employee_title" property="employeeTitle"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.codespace.infrastructure.persistent.po.Employee"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO employee (employee_number, employee_name, employee_level, employee_title, create_time, update_time)
        VALUES (#{employeeNumber}, #{employeeName}, #{employeeLevel}, #{employeeTitle}, #{createTime}, #{updateTime})
    </insert>

    <select id="queryByEmployeeNumber" parameterType="java.lang.String" resultMap="EmployeeMap">
        SELECT id, employee_number, employee_name, employee_level, employee_title, create_time, update_time
        FROM employee
        WHERE employee_number = #{employeeNumber}
    </select>

</mapper>
